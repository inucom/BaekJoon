#include <iostream>
using namespace std;

int main() {
	int a, b;
	cin >> a >> b;
	int c, d;
	for (int i = 0; i < a; i++) {
		for (int j = 0; j < b; j++) {
			cin >> c;
		}
	}
	cout << "비와이";
}